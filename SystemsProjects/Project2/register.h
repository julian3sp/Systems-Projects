#include <stdio.h>
/***************************************************
* void print_item(int rows, int cols, int data[][cols]);
* Takes the user input of what item they want to print and prints the monthly sales of that item
*
* Parameters:
* -rows: number of rows in an array
* -cols: number of cols in an array
* -data: array of sales information
* Returns:
* void
************************************************/
void print_item(int rows, int cols, int data[][cols]);
/***************************************************
* void print_item(int rows, int cols, int data[][cols]);
* Determines the new position of the hare after 1 second
*
* Parameters:
* -rows: number of rows in an array
* -cols: number of cols in an array
* -data: array of sales information
* Returns:
* void
************************************************/
void total_sales(int rows, int cols, int data[][cols]);
/***************************************************
* void print_item(int rows, int cols, int data[][cols]);
* Determines the new position of the hare after 1 second
*
* Parameters:
* -rows: number of rows in an array
* -cols: number of cols in an array
* -data: array of sales information
* Returns:
* void
************************************************/
void top_overall(int rows, int cols, int data[][cols]);



